import seasonConfig from "../config/seasons.json";

// ── Config ──────────────────────────────────────────────
// Season order is configured in config/seasons.json. First entry = current season.
export const SEASONS = seasonConfig.seasons;

export const CONFIG = {
  teamName: "UBC Knights",
  established: "2023",
};

